[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ua433-mesh-lab"
version = "0.1.0"
description = "A small deterministic adaptive LoRa-style mesh simulator"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

